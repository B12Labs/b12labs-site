import type { Metadata } from "next";
import "./globals.css";
import { Nav } from "@/components/Nav";
import { Footer } from "@/components/Footer";

export const metadata: Metadata = {
  title: "B12 Labs — Research for AI-native software",
  description:
    "B12 Labs is the research lab behind BOSS: an AI-native operating system for creators, SMBs, and enterprises. We build cognitive infrastructure, agent frameworks, and open-source memory systems.",
  icons: { icon: "/favicon.svg" },
  openGraph: {
    title: "B12 Labs",
    description: "Research for AI-native software.",
    type: "website",
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="" />
        <link
          href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className="min-h-screen flex flex-col">
        <Nav />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
